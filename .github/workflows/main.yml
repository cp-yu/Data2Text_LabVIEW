name: Build DLL

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.8'
        architecture: 'x86'  # 使用32位Python环境

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install cffi

    - name: Build 32-bit DLL
      run: |
        python build_dll.py

    - name: Upload DLL
      uses: actions/upload-artifact@v2
      with:
        name: dll-files
        path: ./dlltest.*  # 上传生成的DLL文件和其他相关文件
